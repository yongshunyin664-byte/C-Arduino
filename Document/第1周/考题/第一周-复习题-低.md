# 第一周知识点复习题（简单版）

说明：本套试题共30题，按“选择题、判断题、填空题、纠错题、理论简答题”五种题型编排，覆盖第一周全部基础知识点，适合题海训练和基础巩固。

## 1. 选择题

### 1.1 单选题

1. C语言中用于输出内容的函数是哪个？
- A. scanf()
- B. printf()
- C. gets()
- D. getchar()

答题框：
```
你的原始答案：

【修正答案】B
```

2. 下面哪个变量名是合法的？
- A. 2num
- B. int
- C. student_age
- D. my-name

答题框：
```
你的原始答案：

【修正答案】C
```

3. C语言中表示整数类型的关键字是哪个？
- A. char
- B. int
- C. float
- D. double

答题框：
```
你的原始答案：

【修正答案】B
```

4. 下列哪种注释写法是正确的？
- A. `# 这是注释`
- B. `// 这是注释`
- C. `@@ 这是注释`
- D. `&& 这是注释`

答题框：
```
你的原始答案：

【修正答案】B
```

5. `#define PI 3.14` 中，`PI` 是什么？
- A. 变量
- B. 函数名
- C. 宏名
- D. 数组名

答题框：
```
你的原始答案：

【修正答案】C
```

### 1.2 多选题

6. 下列选项中，哪些是C语言中常见的数据类型？（多选）
- A. int
- B. float
- C. char
- D. printf

答题框：
```
你的原始答案：

【修正答案】A、B、C
```

7. 下列选项中，哪些属于 Arduino 程序中常用的函数？（多选）
- A. setup()
- B. loop()
- C. digitalWrite()
- D. scanf()

答题框：
```
你的原始答案：

【修正答案】A、B、C
```

8. 下列选项中，哪些是正确的注释方式？（多选）
- A. `// 这是注释`
- B. `/* 这是注释 */`
- C. `# 这是注释`
- D. `<!-- 这是注释 -->`

答题框：
```
你的原始答案：

【修正答案】A、B
```

## 2. 判断题

9. C语言中的变量名区分大小写。（对/错）

答题框：
```
你的原始答案：

【修正答案】对
```

10. 注释内容会被编译器执行。（对/错）

答题框：
```
你的原始答案：

【修正答案】错
```

11. `3 / 2` 的结果是 `1.5`。（对/错）

答题框：
```
你的原始答案：

【修正答案】错
```

12. `scanf()` 的作用是输入数据。（对/错）

答题框：
```
你的原始答案：

【修正答案】对
```

13. `delay(1000);` 表示延时 1 秒。（对/错）

答题框：
```
你的原始答案：

【修正答案】对
```

14. Arduino程序中，`setup()` 函数只执行一次。（对/错）

答题框：
```
你的原始答案：

【修正答案】对
```

## 3. 填空题

15. C语言中表示字符类型的关键字是________。

答题框：
```
你的原始答案：

【修正答案】char
```

16. `printf("%f", x);` 中的 `%f` 用于输出________类型的数据。

答题框：
```
你的原始答案：

【修正答案】浮点数
```

17. `#define PI 3.14` 中，`PI` 是一个________。

答题框：
```
你的原始答案：

【修正答案】宏名
```

18. 用于从键盘读取整数并存入变量 `a` 的代码是________。

答题框：
```
你的原始答案：

【修正答案】scanf("%d", &a);
```

19. Arduino中，`HIGH` 表示________，`LOW` 表示________。

答题框：
```
你的原始答案：

【修正答案】高电平、低电平
```

20. 在C语言中，表示“取模”的运算符是________。

答题框：
```
你的原始答案：

【修正答案】%
```

## 4. 纠错题

21. 请指出并改正下面的错误：
```c
int 2num = 10;
```

答题框：
```
你的原始答案：

【修正答案】变量名错误，不能以数字开头；应改为 `int num2 = 10;`。
```

22. 请指出并改正下面的错误：
```c
scanf("%d", a);
```

答题框：
```
你的原始答案：

【修正答案】输入的值没有连接到 a，需改为 `&a`。
```

23. 请指出并改正下面的错误：
```c
printf("%d", 3.14);
```

答题框：
```
你的原始答案：

【修正答案】格式控制符 `%d` 不适合输出浮点数，应改为 `%f`。
```

24. 请指出并改正下面的错误：
```c
pinMode(LED_BUILTIN, OUTPUT);
```

答题框：
```
你的原始答案：

【修正答案】这段代码本身是正确的；如果需要完整使用，通常会配合 `pinMode()` 进行引脚设置。
```

## 5. 理论简答题

25. 简述 `printf()` 和 `scanf()` 的作用区别。

答题框：
```
你的原始答案：

【修正答案】printf() 用于把数据输出到屏幕；
scanf() 用于从键盘读取数据并存入变量。
```

26. 简述 `#define` 的用途。

答题框：
```
你的原始答案：

【修正答案】给一个固定值定义一个宏，方便后续调用和修改。
```

27. 为什么要在程序中使用注释？

答题框：
```
你的原始答案：

【修正答案】方便后续理解代码，便于阅读和维护。
```

28. 简述 Arduino 中 `setup()` 和 `loop()` 的作用。

答题框：
```
你的原始答案：

【修正答案】setup() 只执行一次，用于初始化；
loop() 会持续循环执行，用于控制硬件。
```

29. 为什么在 Arduino 程序中要先调用 `pinMode()`？

答题框：
```
你的原始答案：

【修正答案】因为引脚需要先指定输入或输出模式，才能正常工作。
```

30. 简述 `delay()` 的作用及其常见单位。

答题框：
```
你的原始答案：

【修正答案】delay() 用于延时，常见单位是毫秒，1000 表示 1 秒。
```

---

## 参考答案（仅供教师/讲解使用）

> 请将下面内容与题目区分开，学生作答请写在上方的“答题框”中。

### 单选题
1. B  2. C  3. B  4. B  5. C

### 多选题
6. A、B、C  7. A、B、C  8. A、B

### 判断题
9. 对 10. 错 11. 错 12. 对 13. 对 14. 对

### 填空题
15. char 16. 浮点 17. 宏名 18. `scanf("%d", &a);` 19. 高电平/低电平 20. `%`

### 纠错题
21. `int num2 = 10;`
22. `scanf("%d", &a);`
23. `printf("%f", 3.14);`
24. 这段代码本身是正确的；如果需要完整使用，通常会配合 `pinMode()` 进行引脚设置。

### 理论简答题
25. `printf()` 用于输出，`scanf()` 用于输入。
26. `#define` 用于定义宏常量或宏替换，方便统一修改。
27. 注释可以说明程序思路、方便阅读、便于后续维护。
28. `setup()` 只执行一次，用于初始化；`loop()` 循环执行，用于持续控制硬件。
29. 因为引脚要先指定为输入或输出模式，才可以正常工作。
30. `delay()` 用来延时，常见单位是毫秒，`1000` 表示 1 秒。
