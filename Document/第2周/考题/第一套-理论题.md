# 第2周理论题

## 一、选择题

1. 下列哪个关系运算符用于判断不等于？
   - A. `==`
   - B. `!=`
   - C. `>=`
   - D. `<=`

2. 逻辑运算符 `&&` 的含义是：
   - A. 逻辑或
   - B. 逻辑与
   - C. 逻辑非
   - D. 赋值

3. 下列语句中，哪个是合法的 switch 结构？
   - A. `switch(x) { case 1: ... break; default: ... }`
   - B. `switch(x) { if (x==1) ... }`
   - C. `switch x { case 1: ... }`
   - D. `switch(x) case 1: ...`

4. `for (int i = 0; i < 5; i++)` 的循环次数是：
   - A. 4
   - B. 5
   - C. 6
   - D. 无限次

5. 在 C 语言中，`continue` 的作用是：
   - A. 结束整个循环
   - B. 跳出当前分支
   - C. 跳过本次循环，进入下一次迭代
   - D. 重新执行当前语句

## 二、判断题（对的写“T”，错的写“F”）

1. `do...while` 循环先判断条件再执行循环体。 ( )
2. `switch...case` 只能用于整数类型值的判断。 ( )
3. `digitalRead()` 返回值只有 `HIGH` 和 `LOW`。 ( )
4. `analogWrite()` 可以直接控制模拟引脚读取电压。 ( )
5. `tone()` 发出指定频率的声音。 ( )

## 三、简答题

1. 请简述 `if...else if...else` 结构与 `switch...case` 结构的区别。

2. `while` 循环和 `do...while` 循环的主要区别是什么？

3. 在 Arduino 中，为什么要先用 `pinMode(pin, OUTPUT)` 设置 LED 引脚？

4. 说明 `analogRead()` 的返回值范围，以及它通常用来读取什么类型的信号。

5. `break` 在循环中有什么作用？请给出一个简单的说明。